<h1><%=@source.name %></h1>

<% @quotes.each do |quote| %>
    <%= raw(quote.body) %>
  <p>&mdash; <%= link_to quote.source.name, source_path(quote.source) %></p>
  <hr />
<% end %>

<%= paginate @quotes %>
